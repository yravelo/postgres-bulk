package io.ybr.postgresbulk.springdata.repository;

/** Test classpath stand-in for the optional JPA fragment, which is forbidden as a dependency. */
public interface PostgresBulkRepository<T, ID> {}
