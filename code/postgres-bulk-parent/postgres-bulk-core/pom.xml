<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.github.yravelo</groupId>
    <artifactId>postgres-bulk-parent</artifactId>
    <version>${revision}</version>
  </parent>
  <artifactId>postgres-bulk-core</artifactId>
  <name>PostgreSQL Bulk Core</name>
  <description>Framework-independent public contracts and metadata for PostgreSQL bulk operations</description>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>enforce-core-dependency-boundary</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <bannedDependencies>
                  <excludes>
                    <exclude>org.springframework:*</exclude>
                    <exclude>org.springframework.boot:*</exclude>
                    <exclude>org.springframework.data:*</exclude>
                    <exclude>org.hibernate*:*</exclude>
                    <exclude>jakarta.persistence:*</exclude>
                    <exclude>org.postgresql:*</exclude>
                    <exclude>io.micrometer:*</exclude>
                  </excludes>
                  <searchTransitive>true</searchTransitive>
                  <message>postgres-bulk-core must remain independent of frameworks and infrastructure.</message>
                </bannedDependencies>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
